Installation
============
The easiest way to install mars_time is to use pip. Simply use
:code:`pip install mars_time` and you should be good to go!

Alternatively, you could clone the repo and install it locally with
:code:`<path to python> -m pip install <path to setup.py>`.
